rentloop/web/
│
├── src/
│   ├── api/
│   │   ├── client.ts                       # Axios instance — baseURL from env, JWT interceptor, 401 redirect
│   │   ├── types.ts                        # All interfaces mirroring Go response shapes
│   │   ├── auth.ts                         # register(), login(), refresh(), logout()
│   │   ├── properties.ts                   # list(), create(), update(), remove()
│   │   ├── units.ts                        # list(), create(), update(), remove()
│   │   ├── tenants.ts                      # get(), assign(), update(), moveOut()
│   │   ├── payments.ts                     # list(), triggerSTK(), exportCSV()
│   │   ├── dashboard.ts                    # getSummary(propertyId)
│   │   └── billing.ts                      # getSubscription(), payNow()
│   │
│   ├── stores/
│   │   ├── auth.ts                         # Pinia — token in memory, landlord profile, isAuthenticated
│   │   ├── property.ts                     # Pinia — active property ID, properties list
│   │   └── ui.ts                           # Pinia — sidebar state, toast queue, loading flags
│   │
│   ├── composables/
│   │   ├── useAuth.ts                      # login, logout, redirect logic
│   │   ├── useDashboard.ts                 # TanStack Query — getSummary, refetchInterval: 60_000
│   │   ├── usePayments.ts                  # paginated list, filters (unit, month)
│   │   ├── useSTKPush.ts                   # trigger, loading state, success/error
│   │   ├── useReceipt.ts                   # jsPDF — generateAndDownload(payment)
│   │   ├── useSubscription.ts              # get status, trigger manual payment
│   │   └── useToast.ts                     # show(message, type), auto-dismiss
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.vue                # authenticated wrapper — sidebar + topbar + RouterView
│   │   │   ├── Sidebar.vue                 # nav links, property switcher, subscription badge, logout
│   │   │   ├── Topbar.vue                  # property name, month, notification bell
│   │   │   └── MobileNav.vue               # bottom tab bar for small screens
│   │   │
│   │   ├── dashboard/
│   │   │   ├── CollectionMeter.vue         # KES collected / expected + % progress bar
│   │   │   ├── StatusSummaryCards.vue      # paid / partial / overdue counts
│   │   │   ├── UnitGrid.vue                # responsive grid of UnitCard
│   │   │   ├── UnitCard.vue                # unit code, tenant name, amount, status badge, action
│   │   │   └── RequestAllButton.vue        # bulk STK Push to all overdue tenants
│   │   │
│   │   ├── payments/
│   │   │   ├── PaymentTable.vue            # filterable table with receipt download per row
│   │   │   ├── PaymentFilters.vue          # unit select + month picker
│   │   │   └── ExportButton.vue            # CSV download trigger
│   │   │
│   │   ├── tenants/
│   │   │   ├── TenantList.vue              # list with balance summary + add button
│   │   │   ├── TenantCard.vue              # name, phone, arrears, move-in, actions
│   │   │   ├── TenantForm.vue              # add/edit — name, phone, unit select, rent, deposit
│   │   │   ├── TenantHistory.vue           # slide-over — full payment history per tenant
│   │   │   └── ArrearsWidget.vue           # red badge, total outstanding
│   │   │
│   │   ├── properties/
│   │   │   ├── PropertyForm.vue            # name, location, paybill number
│   │   │   └── UnitForm.vue                # unit code, monthly rent
│   │   │
│   │   ├── billing/
│   │   │   ├── SubscriptionBanner.vue      # trial ending / paused warning at top of dashboard
│   │   │   ├── BillingStatus.vue           # plan name, next billing date, amount
│   │   │   └── PayNowModal.vue             # confirm → triggers STK Push for subscription
│   │   │
│   │   ├── stk/
│   │   │   └── STKModal.vue                # confirm dialog — tenant name, amount, unit before firing
│   │   │
│   │   └── ui/
│   │       ├── AppButton.vue               # primary / secondary / danger / ghost / loading state
│   │       ├── AppInput.vue                # label, error, helper text
│   │       ├── AppSelect.vue               # styled select
│   │       ├── AppModal.vue                # accessible dialog — focus trap, ESC close
│   │       ├── AppBadge.vue                # paid=green / partial=amber / overdue=red / paused=grey
│   │       ├── AppSpinner.vue
│   │       ├── AppToast.vue                # auto-dismiss, stacks multiple
│   │       ├── AppTable.vue                # slot-based reusable table
│   │       ├── AppEmptyState.vue           # icon + message + optional CTA
│   │       └── AppCard.vue                 # bordered content container
│   │
│   ├── pages/
│   │   ├── auth/
│   │   │   ├── LoginPage.vue
│   │   │   └── RegisterPage.vue
│   │   ├── DashboardPage.vue
│   │   ├── PaymentsPage.vue
│   │   ├── TenantsPage.vue
│   │   ├── TenantDetailPage.vue
│   │   ├── PropertiesPage.vue
│   │   ├── SettingsPage.vue                # paybill config, reminder day, profile, password
│   │   ├── BillingPage.vue                 # subscription status, payment history, upgrade
│   │   ├── NotFoundPage.vue
│   │   └── ErrorPage.vue
│   │
│   ├── router/
│   │   ├── index.ts                        # createRouter, history mode
│   │   └── guards.ts                       # requireAuth, requireActiveSubscription
│   │
│   ├── utils/
│   │   ├── format.ts                       # formatKES(cents), formatDate(), formatPhone()
│   │   ├── format.test.ts
│   │   ├── receipt.ts                      # jsPDF receipt builder — pure client, no server call
│   │   └── receipt.test.ts
│   │
│   ├── App.vue
│   └── main.ts                             # createApp → Pinia → Router → TanStack Query → mount
│
├── tests/
│   ├── unit/
│   │   ├── components/
│   │   │   ├── UnitCard.test.ts            # correct badge per status
│   │   │   ├── CollectionMeter.test.ts     # percentage calculation, colour thresholds
│   │   │   ├── TenantForm.test.ts          # required field validation
│   │   │   ├── STKModal.test.ts            # confirm fires useSTKPush, cancel closes
│   │   │   └── SubscriptionBanner.test.ts  # shows for trial/paused, hidden for active
│   │   └── composables/
│   │       ├── useAuth.test.ts             # success sets token, failure shows toast
│   │       ├── useSTKPush.test.ts          # loading / success / error states
│   │       └── useReceipt.test.ts          # correct PDF fields from payment object
│   └── e2e/
│       └── .gitkeep                        # Playwright — Phase 3
│
├── index.html
├── vite.config.ts                          # dev proxy: /api → :8080, /webhooks → :8080
├── vitest.config.ts                        # happy-dom, coverage v8
├── tailwind.config.ts                      # brand colours: navy, success, warning, danger
├── tsconfig.json                           # strict, @ alias → src/
├── .eslintrc.cjs
├── .prettierrc
└── package.json