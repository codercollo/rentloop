rentloop/
├── cmd/
│   └── server/
│       └── main.go                    ← boot: DB, routes, cron, server
│
├── internal/
│   │
│   ├── models/                        ← shared types, no logic, no imports
│   │   ├── landlord.go                ← Landlord, AdminUser structs
│   │   ├── unit.go                    ← Unit, Tenant structs
│   │   ├── payment.go                 ← Payment, PaymentStatus enum
│   │   ├── billing.go                 ← SubscriptionStatus enum: active | grace | suspended
│   │   └── errors.go                  ← ErrNotFound, ErrUnauthorised, ErrAccountSuspended
│   │
│   ├── auth/                          ← admin auth (JWT)
│   │   ├── handler.go                 ← POST /admin/register, /login, /activate
│   │   ├── service.go                 ← bcrypt hash, JWT sign/verify, activation
│   │   ├── repository.go              ← admin_users CRUD
│   │   ├── middleware.go              ← RequireAdmin JWT middleware
│   │   └── service_test.go            ← unit tests: hash, token, activation
│   │
│   ├── admin/                         ← dashboard data + HTMX templates
│   │   ├── handler.go                 ← GET /admin/dashboard, /clients, /payments
│   │   ├── service.go                 ← aggregate stats, filter logic
│   │   ├── repository.go              ← dashboard queries (counts, totals)
│   │   └── service_test.go            ← unit tests: stat aggregation
│   │
│   ├── mpesa/
│   │   ├── handler.go                 ← POST /mpesa/c2b/callback
│   │   ├── validator.go               ← IP allowlist, payload check
│   │   ├── types.go
│   │   └── handler_test.go            ← unit tests: callback parsing, IP block
│   │
│   ├── landlord/                      ← landlord CRUD
│   │   ├── handler.go                 ← onboarding endpoints
│   │   ├── service.go                 ← register landlord, assign paybill
│   │   ├── repository.go              ← landlords + units SQL
│   │   └── service_test.go
│   │
│   ├── billing/                       ← subscription lifecycle
│   │   ├── handler.go                 ← POST /billing/callback (subscription C2B)
│   │   ├── service.go                 ← status transitions, amount calc, grace logic
│   │   ├── repository.go              ← subscription_payments CRUD, status updates
│   │   ├── cron.go                    ← 1st of month: flag expired → grace
│   │   │                                 day 6: grace → suspended
│   │   │                                 on payment: suspended/grace → active
│   │   ├── middleware.go              ← CheckSubscription: gate bot + notifier
│   │   └── service_test.go            ← test: active→grace→suspended transitions
│   │                                     test: payment reactivates account
│   │                                     test: free tier never suspended
│   │                                     test: unit_count × 50 billing calc
│   │
│   ├── matcher/
│   │   ├── matcher.go
│   │   └── matcher_test.go            ← test: "4b", "4 B", "unit4B" all match
│   │
│   ├── ledger/
│   │   ├── service.go                 ← payment logic lives here
│   │   ├── repository.go              ← payment SQL
│   │   └── service_test.go            ← test: partial pay, overpay, idempotency
│   │
│   ├── bot/
│   │   ├── handler.go                 ← checks billing.CheckSubscription before every command
│   │   ├── commands.go
│   │   ├── digest.go
│   │   └── commands_test.go           ← test: LIST, REMIND, RECEIPT parsing
│   │                                     test: suspended account returns lockout message
│   │
│   ├── notifier/
│   │   ├── whatsapp.go
│   │   └── sms.go
│   │
│   ├── onboarding/
│   │   ├── handler.go
│   │   ├── csv_parser.go
│   │   └── csv_parser_test.go         ← test: bad phone, missing rent, duplicates
│   │
│   ├── receipt/
│   │   ├── generator.go
│   │   ├── storage.go
│   │   └── templates/
│   │       └── receipt.html
│   │
│   ├── config/
│   │   └── config.go
│   │
│   └── db/
│       ├── db.go
│       └── migrations/
│           ├── 001_init.sql           ← landlords, units, payments tables
│           ├── 002_receipts.sql       ← receipt_url, month_key on payments
│           ├── 003_admin_users.sql    ← admin_users table
│           └── 004_billing.sql        ← subscription_status, billing_cycle_end
│                                         on landlords + subscription_payments table
│
├── web/                               ← admin UI (Go html/template + HTMX)
│   ├── templates/
│   │   ├── base.html                  ← layout: sidebar, nav, head
│   │   ├── login.html
│   │   ├── register.html
│   │   ├── activate.html              ← token link from email
│   │   ├── dashboard.html             ← stats cards + recent payments
│   │   ├── clients.html               ← table: all landlords + subscription status
│   │   ├── client_detail.html         ← one landlord: units, payment history, billing
│   │   └── payments.html              ← all payments, filterable
│   └── static/
│       ├── css/
│       │   └── admin.css
│       └── js/
│           └── htmx.min.js
│
├── pkg/
│   └── httputil/
│       └── respond.go
│
├── .github/
│   └── workflows/
│       ├── test.yml                   ← on: push → go test ./...
│       └── deploy.yml                 ← on: push to main → scp + restart
│
├── deployments/
│   ├── nginx.conf
│   └── rentloop.service
│
├── .env.example
├── .env
├── .gitignore
├── Makefile
├── go.mod
└── go.sum