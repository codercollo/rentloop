
rentloop/
├── cmd/
│   ├── server/
│   │   └── main.go                    ✓ BUILT — Twilio wiring, all routes mounted
│   │
│   └── migrate/
│       └── main.go                    ✓ BUILT — SQL migration runner
│
├── internal/
│   │
│   ├── models/                        ✓ BUILT — domain types, no logic
│   │   ├── landlord.go                ← Landlord, AdminUser
│   │   ├── unit.go                    ← Unit
│   │   ├── payment.go                 ← Payment, PaymentStatus, MonthlyBalance
│   │   ├── subscription.go            ← SubscriptionStatus, SubscriptionPayment
│   │   ├── agent.go                   ← Agent
│   │   └── errors.go                  ← sentinel errors
│   │
│   ├── mpesa/                         ✓ BUILT — Daraja C2B webhook
│   │   ├── handler.go                 ← POST /mpesa/c2b/callback
│   │   │                                 nil-safe, returns 200 immediately, async
│   │   ├── validator.go               ← Safaricom IP allowlist + payload check
│   │   ├── types.go                   ← C2B structs + successResponse
│   │   └── handler_test.go            ← 10 tests: IP block, payload, async, duplicate
│   │
│   ├── matcher/                       ✓ BUILT — account ref → unit resolution
│   │   ├── matcher.go                 ← normalise: "4b" "4 B" "UNIT4B" → "4B"
│   │   └── matcher_test.go            ← 11 tests: variants, scoping, isolation
│   │
│   ├── ledger/                        ✓ BUILT — payment recording
│   │   ├── service.go                 ← paid / partial / overpaid / idempotency
│   │   ├── repository.go              ← payments SQL, GetUnitByRef, GetByPaybill
│   │   │                                 nullable unit_id for unmatched payments
│   │   └── service_test.go            ← 8 tests: all status paths + duplicate
│   │
│   ├── notifier/                      ✓ BUILT — outbound messaging via Twilio
│   │   ├── twilio.go                  ← WhatsApp + SMS: landlord, tenant, reminders
│   │   │                                 SendRaw (bot), SendRawSMS (onboarding)
│   │   ├── twilio_test.go             ← 5 tests: httptest server, all message types
│   │   ├── whatsapp.go                ← AT WhatsApp (kept, not wired in main.go)
│   │   └── sms.go                     ← AT SMS (kept, not wired in main.go)
│   │
│   ├── bot/                           ✓ BUILT — WhatsApp + SMS command bot
│   │   ├── handler.go                 ← Twilio + AT form-encoded inbound
│   │   │                                 strips whatsapp: prefix, async processing
│   │   ├── sms_handler.go             ← POST /bot/sms, form-encoded, async
│   │   ├── service.go                 ← identify sender, subscription gate,
│   │   │                                 HandleSMS + normaliseSMSPhone
│   │   ├── commands.go                ← LIST, REMIND, RECEIPT, TOTAL, HISTORY,
│   │   │                                 MARK (ledger write), CLAIM, REPLACE
│   │   │                                 (soft-delete), SET RENT (DB update)
│   │   ├── agent_commands.go          ← CLIENTS, LIST {name}, REMIND {name},
│   │   │                                 RECEIPT {name} {unit}, TOTAL ALL
│   │   ├── repository.go              ← all bot SQL + CreateLandlord,
│   │   │                                 UpdateUnitCount, ReplaceUnitTenant,
│   │   │                                 UpdateExpectedRent, InsertManualPayment
│   │   ├── digest.go                  ← robfig/cron: 6 PM unpaid summary
│   │   └── commands_test.go           ← 12 tests: all commands + stubs
│   │
│   ├── onboarding/                    ✓ BUILT — JOIN flow + CSV bulk upload
│   │   ├── handler.go                 ← HandleJoin, HandleBulkAdd
│   │   │                                 welcome via smsSender (Twilio SMS)
│   │   ├── csv_parser.go              ← download URL, parse, validate rows
│   │   │                                 normalise phone + unit ref per row
│   │   └── csv_parser_test.go         ← 13 tests: all validation paths
│   │
│   ├── landlord/                      ✗ NOT BUILT — planned v1.1
│   │   └── (empty)
│   │
│   ├── agent/                         ✗ NOT BUILT — planned v1.1
│   │   └── (empty)
│   │
│   ├── billing/                       ✗ NOT BUILT — planned v1.1
│   │   └── (empty)                       active → grace → suspended cron
│   │
│   ├── auth/                          ✗ NOT BUILT — planned v1.1
│   │   └── (empty)                       admin JWT login
│   │
│   ├── admin/                         ✗ NOT BUILT — planned v1.1
│   │   └── (empty)                       dashboard: MRR, clients, payments
│   │
│   ├── receipt/                       ✗ NOT BUILT — planned v2
│   │   └── (empty)                       PDF generation + DO Spaces upload
│   │
│   ├── config/
│   │   └── config.go                  ✓ BUILT — env load + validate
│   │                                     includes Twilio + AT credentials
│   │
│   └── db/
│       ├── db.go                      ✓ BUILT — pgx pool, ping on startup
│       └── migrations/
│           ├── 001_init.sql           ✓ landlords, units, payments + indexes
│           │                             unit_id nullable for unmatched payments
│           ├── 002_receipts.sql       ✓ receipts table
│           ├── 003_admin_users.sql    ✓ admin_users
│           ├── 004_billing.sql        ✓ subscription_status, billing_cycle_end,
│           │                             unit_count on landlords
│           │                             + subscription_payments table
│           └── 005_agents.sql         ✓ agents table + agent_id FK on landlords
│
├── web/                               ✗ NOT BUILT — planned v1.1 (admin UI)
│   ├── templates/
│   │   ├── base.html
│   │   ├── login.html
│   │   ├── register.html
│   │   ├── activate.html
│   │   ├── dashboard.html
│   │   ├── clients.html
│   │   ├── client_detail.html
│   │   ├── agents.html
│   │   ├── agent_detail.html
│   │   └── payments.html
│   └── static/
│       ├── css/admin.css
│       └── js/htmx.min.js
│
├── pkg/
│   └── httputil/
│       └── respond.go                 ← JSON helpers, error wrapper
│
├── sandbox/                           ← local testing scripts, not deployed
│   └── sms/
│       └── activate/                  ← AT sandbox activation helper
│
├── .github/
│   └── workflows/
│       ├── test.yml                   ✓ BUILT — push → go test ./... -race
│       │                                 spins up Postgres 15 in CI
│       └── deploy.yml                 ✓ BUILT — push to main → build linux binary
│                                          → scp to droplet → migrate → restart
│                                          → health check
│
├── deployments/
│   ├── nginx.conf                     ✓ BUILT — reverse proxy + SSL termination
│   │                                     copy to /etc/nginx/sites-available/
│   └── rentloop.service               ✓ BUILT — systemd unit, auto-restart,
│                                         non-root user, EnvironmentFile
│
├── Dockerfile                         ✓ BUILT — two-stage: builder + alpine
│                                         for Railway/Render if needed later
├── .dockerignore                      ✓ BUILT — excludes .env from image
├── .env.example                       ← all required vars documented
├── .env                               ← gitignored, never committed
├── .gitignore
├── Makefile
├── go.mod
└── go.sum

