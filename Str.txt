rentloop/                              # monorepo root
│
├── .github/
│   └── workflows/
│       ├── ci.yml                     # test on every PR
│       └── deploy.yml                 # build + ship on push to main
│
├── cmd/                               # ── GO BACKEND ──────────────────
│   ├── api/                           # HTTP server binary
│   └── worker/                        # background worker binary
│
├── internal/                          # private backend packages
│   ├── config/                        # env + viper config loader
│   ├── db/                            # database layer
│   │   ├── migrations/                # SQL migration files
│   │   ├── queries/                   # sqlc query definitions
│   │   └── generated/                 # sqlc output — never touch
│   ├── domain/                        # business logic — payments, arrears, billing
│   ├── mpesa/                         # Daraja client, C2B, STK, token, IP list
│   ├── billing/                       # subscription scheduler, collector, retry
│   ├── handlers/                      # HTTP handlers — one file per resource
│   ├── middleware/                    # JWT, IP guard, idempotency, rate limit
│   ├── queue/                         # Redis Stream producer + consumer
│   ├── scheduler/                     # cron jobs — reminders, arrears, cleanup
│   ├── sms/                           # Africa's Talking wrapper + templates
│   └── server/                        # chi router setup + route registration
│
├── pkg/                               # reusable packages — no internal imports
│   ├── money/                         # KES type (int64 cents) — never float
│   ├── phone/                         # Kenyan number normalisation
│   ├── token/                         # JWT generate + parse
│   ├── apperrors/                     # typed domain errors
│   └── ctxkeys/                       # typed context keys
│
├── tests/
│   └── integration/                   # end-to-end flow tests (testcontainers)
│       └── testhelpers/               # PG + Redis containers, fixtures, mpesa mock server
│
├── web/                               # ── VUE 3 FRONTEND ──────────────
│   ├── src/
│   │   ├── api/                       # Axios endpoint wrappers + shared types
│   │   ├── stores/                    # Pinia — auth, property, ui
│   │   ├── composables/               # useAuth, useDashboard, useSTKPush, useReceipt
│   │   ├── components/
│   │   │   ├── layout/                # AppShell, Sidebar, Topbar, MobileNav
│   │   │   ├── dashboard/             # CollectionMeter, UnitGrid, UnitCard
│   │   │   ├── payments/              # PaymentTable, PaymentFilters, ExportButton
│   │   │   ├── tenants/               # TenantList, TenantCard, TenantForm, TenantHistory
│   │   │   ├── properties/            # PropertyForm, UnitForm
│   │   │   ├── billing/               # SubscriptionBanner, BillingStatus, PayNowModal
│   │   │   ├── stk/                   # STKModal
│   │   │   └── ui/                    # AppButton, AppInput, AppModal, AppBadge, AppTable ...
│   │   ├── pages/
│   │   │   ├── auth/                  # LoginPage, RegisterPage
│   │   │   ├── DashboardPage.vue
│   │   │   ├── PaymentsPage.vue
│   │   │   ├── TenantsPage.vue
│   │   │   ├── TenantDetailPage.vue
│   │   │   ├── PropertiesPage.vue
│   │   │   ├── SettingsPage.vue
│   │   │   ├── BillingPage.vue
│   │   │   └── errors/                # NotFoundPage, ErrorPage
│   │   ├── router/                    # Vue Router + navigation guards
│   │   └── utils/                     # formatKES, formatDate, receipt PDF builder
│   └── tests/
│       ├── unit/                      # Vitest — components + composables
│       └── e2e/                       # Playwright — Phase 3
│
├── deployments/
│   ├── nginx/                         # rentloop.conf — reverse proxy config
│   ├── systemd/                       # rentloop-api.service, rentloop-worker.service
│   └── docker-compose.yml             # dev only — postgres + redis + mailhog
│
├── scripts/
│   ├── setup_db.sh                    # createdb + migrate up
│   ├── seed.sh                        # dev seed data
│   ├── simulate_c2b.sh                # fake C2B webhook for local testing
│   └── deploy.sh                      # build → scp → migrate → restart
│
├── docs/
│   ├── api.md                         # endpoint reference
│   ├── mpesa_setup.md                 # Daraja registration walkthrough
│   ├── local_dev.md                   # run from zero in 5 minutes
│   └── architecture.md               # system design decisions
│
├── .env.example                       # all variables documented, no values
├── .gitignore
├── .golangci.yml                      # linter rules
├── Makefile                           # single entry point for all commands
├── go.mod                             # Go module root
├── go.sum
└── README.md